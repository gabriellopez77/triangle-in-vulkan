#include "Math.h"

#include <cmath>


namespace math {
    float cos(f32 value) { return std::cos(value); }

    float sin(f32 value) { return std::sin(value); }

    float tan(f32 value) { return std::tan(value); }
}

